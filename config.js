/**
 * ============================================
 *  智慧树自动化脚本 — 配置文件
 *  所有可调参数集中在这里，方便修改
 * ============================================
 */
module.exports = {

  // ─── 播放设置 ───────────────────────────────
  playbackRate: 1.5,           // 播放倍速（智慧树最高支持 2.0）
  checkInterval: 2000,         // 主循环检测间隔（毫秒）

  // ─── 模拟人类行为 ───────────────────────────
  randomDelayMin: 600,         // 随机等待最小值（毫秒）
  randomDelayMax: 2500,        // 随机等待最大值（毫秒）
  mouseMoveChance: 0.3,        // 每次循环模拟鼠标移动的概率（0~1）

  // ─── 卡顿检测 ───────────────────────────────
  stallThreshold: 25,          // 当前时间无变化超过此秒数视为卡住
  maxRefreshRetries: 3,        // 最大刷新重试次数
  refreshCooldown: 15000,      // 刷新后等待时间（毫秒）

  // ─── 浏览器设置 ─────────────────────────────
  headless: false,             // 必须为 false，可视化运行
  viewport: { width: 1920, height: 1080 },

  // ─── 固定坐标模式（答题弹窗） ─────────────────
  // 以浏览器视口左上角为原点，非屏幕原点
  // 打开答题弹窗后截图，用截图工具测量坐标
  // 调整方式：数值越大越靠右/靠下，越小越靠左/靠上
  fixedCoords: {
    option: { x: 900, y: 570 },     // 选项点击位置
    close: { x: 919, y: 725 },    // 关闭按钮位置
    yRange: 30,                       // 选项二次点击的上下偏移范围(px)
    offset: { x: -80, y: -50 },      // 坐标修正偏移(px)，点击偏右下时设为负值向左上调整
  },

  // ─── 按钮文本识别库 ─────────────────────────
  // 按功能分组，每一组包含多个匹配模式
  // 脚本会逐个尝试匹配页面按钮文本
  buttonTexts: {
    // 播放相关
    play: [
      '播放', '▶', '►', '继续播放',
      'play', 'Play',
    ],
    // 暂停相关（用于检测当前状态）
    pause: [
      '暂停', '❚❚', '⏸',
      'pause', 'Pause',
    ],
    // 下一节 / 下一集
    next: [
      '下一节', '下一集', '下一个', '下一章', '下一個',
      '继续下一节', '进入下一节', '继续', '接下来',
      '→', '►', '▶', '≫', '»',
    ],
    // 继续学习（断点续播弹窗）
    resume: [
      '继续学习', '继续播放', '从上次继续',
      '继续', 'resume',
    ],
    // 确认 / 提交（互动题确认）
    confirm: [
      '确认', '确定', '提交', '确定提交',
      'confirm', 'submit', 'OK',
    ],
    // 关闭
    close: [
      '关闭', '关闭窗口', '×', '✕', '✖', 'close',
      '完成', '完成关闭',
    ],
    // 知道了（提示弹窗确认）
    acknowledge: [
      '知道了', '好的', '我了解了',
      'got it', 'ok',
    ],
    // 速度控制
    speed: [
      '倍速', '速度', '速率', 'speed',
      '1.0', '1.5', '2.0',
    ],
  },

  // ─── 互动题检测关键词 ───────────────────────
  // 用于判断一个弹窗是否是课堂互动题
  interactionKeywords: [
    '单选题', '多选题', '判断题',
    '请选择', '请回答', '互动',
    '课堂互动', '投票',
  ],

  // ─── 非视频页面检测 ─────────────────────────
  // 出现在单元检测 / 考试页面时停止自动化
  testPageKeywords: [
    '单元检测', '章节测验', '章节测试',
    '考试', '测验', '作业', '课后作业',
    'test', 'exam', 'quiz', 'assignment',
  ],

  // ─── 日志格式 ───────────────────────────────
  log: {
    showTimestamp: true,
    showLevel: true,
  },
};
